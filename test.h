#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int get_image_src_offset(FILE* bmp_offset);
int get_message_input_length(FILE* fp);
void fromByteToBitsDisplay(char);
int get_wanted_bit(char, int);
